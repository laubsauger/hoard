// T38 — render/scene barrel: the city-block scene the engine draws + its pure day/night helper.

export { BlockScene } from './blockScene';
export {
  computeSkyState,
  type SkyState,
  type SkyLightingInput,
  type SkyWeatherInput,
} from './sky';
